import org.apache.commons.lang3.math.NumberUtils;

public class Example {
    public static void main(String[] args) {
        // Converts a String to a number, throwing an exception if the conversion fails.
        int number = NumberUtils.toInt("100");  // 100
    }
}